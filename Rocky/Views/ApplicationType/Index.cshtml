@model IEnumerable<Rocky.Models.ApplicationType>

<div class="container">
	<div class="row pb-4">
		<div class="col-6 text-primary">
			<h2>Application Type List</h2>
		</div>
		<div class="col-6 text-right">
			<a class="btn btn-primary text-light" asp-controller="ApplicationType" asp-action="Create"><i class="fas fa-plus"></i> &nbsp; 
				Create New Application Type</a>
		</div>
	</div>
	@if(Model.Count()>0)
	{
		<table class="table table-bordered table-striped text-center w-100">
		<thead>
			<tr>
				<th>Category Name</th>
			</tr>
		</thead>
		<tbody>
			@foreach(var item in Model)
			{
				<tr>
				<td width="50%">
					@item.Name
				</td>
				<td class="text-center">
					<div class="btn-group w-75" role="group">
			             <a class="btn btn-primary mx-1" asp-controller="ApplicationType" asp-route-Id="@item.Id" asp-action="Edit">
							 <i class="fas fa-edit pt-2"></i></a>
			             <a class="btn btn-danger btn-lg mx-1" asp-controller="ApplicationType" asp-route-Id="@item.Id" asp-action="Delete">
							 <i class="fas fa-trash-alt"></i></a>
					</div>
				</td>
				</tr>
			}
		</tbody>
		</table>
	}
	else
	{
		<p class="text-danger">
			No ApplicationType exists.
		</p>
	}

</div>

